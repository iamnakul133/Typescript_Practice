//const user : (string| number)[] = [1,"hc"]
// tuple - it make sure the order is also preserved in the array
let tuser : [string, number, boolean] 
tuser = ["hc",322, true]

let rgb : [number, number, number] = [223,223,223]

type User = [number, string]
const newUser : User = [223, "nakul"]

// you can overwrite the number with string(exception)

newUser[1] = "high"



